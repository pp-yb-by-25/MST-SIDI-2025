CHAPITRE 1 — Introduction, Architecture & Cycle de vie
🧠 Vrai / Faux (niveau examen)

Android repose sur un noyau Linux modifié pour intégrer la gestion de l’énergie et des processus.
vrai

La couche Hardware Abstraction Layer (HAL) permet à Android de communiquer avec le matériel sans dépendre du fabricant.
vrai

La machine virtuelle ART effectue une compilation AOT (Ahead-Of-Time) lors de l’installation de l’application.
vrai

Dalvik utilisait une compilation JIT (Just-In-Time) au moment de l’exécution.
vrai

Une activité passe toujours par onDestroy() avant d’être supprimée par le système.
vrai

Le système Android peut détruire une activité sans appeler onPause() en cas de manque de mémoire.
vrai

Le fichier AndroidManifest.xml est obligatoire pour toute application Android.
vrai 

🔄 Cycle de vie — Code à compléter (EXAMEN CLASSIQUE)

Complétez l’ordre exact lors du premier lancement :

@Override
protected void onCreate() {
    super.onCreate();
    setContentView(R.layout.activity_main);
}

@Override
protected void onStart() {
    super.onStart();
}

@Override
protected void onResume() {
    super.onResume();
}

❓ Question de réflexion (souvent notée)

Pourquoi Android peut-il détruire une activité sans prévenir le développeur, 
et quel mécanisme permet de restaurer son état ?

Android détruit une activité si y a un manque de mémoire.
Sauvegarde

✅ CHAPITRE 2 — Conception de l’Interface Utilisateur (UI)
🧠 Vrai / Faux (pièges courants)

Toute vue héritant de ViewGroup peut contenir d’autres vues.

wrap_content force une vue à occuper tout l’espace disponible.

ConstraintLayout permet de remplacer plusieurs layouts imbriqués.

Les dimensions exprimées en dp s’adaptent à la densité de l’écran.

Un RelativeLayout est plus performant qu’un LinearLayout imbriqué.

🧩 Attributs XML — Interprétation

Expliquez précisément le rôle de :

android:hint

android:inputType="textEmailAddress"

android:gravity="center"

android:layout_weight

android:padding

📐 Exercice XML (très demandé)

Créez un layout vertical contenant :

un TextView centré

un EditText avec un indice

un Button aligné en bas

(Sans écrire tout le code, indiquez les layouts et attributs clés)

✅ CHAPITRE 3 — Programmation d’une Activité
🧠 Vrai / Faux (examen)

Le fichier R.java est automatiquement régénéré après chaque modification des ressources.

findViewById() retourne toujours un objet non nul.

Un TextWatcher permet de détecter les modifications en temps réel.

Les événements utilisateur sont traités dans le thread principal (UI Thread).

Un NullPointerException est fréquent lors de la récupération d’un composant mal lié.

💻 Code à compléter (Interaction utilisateur)
String texte = edt_note.getText().toString().__________();
float note = Float.__________(texte);

btn_submit.__________(new View.OnClickListener() {
    @Override
    public void onClick(View v) {
        tv_resultat.__________("Résultat : " + note);
        tv_resultat.setVisibility(View.__________);
    }
});

❓ Question théorique

Pourquoi Android interdit-il les opérations longues dans le thread principal ?

✅ CHAPITRE 4 — Communication entre Composants (Intents)
🧠 Vrai / Faux (pièges)

Un Intent implicite spécifie directement la classe cible.

putExtra() permet de transmettre des objets primitifs.

Une activité peut recevoir des données même après sa destruction.

startActivityForResult() est désormais remplacée par l’API Activity Result.

🔄 Code à compléter (Transmission de données)

Envoi :

Intent intent = new Intent(MainActivity.this, Activity2.class);
intent.__________("cle_age", age);
startActivity(intent);


Réception :

int age = getIntent().__________("cle_age", 0);

❓ Question réflexion

Quelle est la différence entre un Intent explicite et implicite, et donnez un cas d’utilisation réel pour chacun.

✅ CHAPITRE 5 — Sauvegarde des Données
🧠 Vrai / Faux (niveau prof strict)

SharedPreferences est adapté pour stocker des objets complexes.

Le stockage interne est supprimé lors de la désinstallation de l’application.

SQLite est embarqué directement dans Android.

Un Cursor permet de naviguer ligne par ligne dans un résultat SQL.

commit() est synchrone contrairement à apply().

🗄️ SQLite — Questions clés

Quelles méthodes doivent obligatoirement être redéfinies dans SQLiteOpenHelper ?

À quoi sert ContentValues ?

Quelle est la différence entre getReadableDatabase() et getWritableDatabase() ?

💾 Code à compléter (SharedPreferences)
SharedPreferences.Editor editor =
        getSharedPreferences("USER_PREF", MODE_PRIVATE).edit();

editor.__________("username", nom);
editor.__________();